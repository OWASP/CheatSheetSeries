# VS Code Configuration File
.vscode
# Audit result files
*.out
# Website generation stuff
TOC.md
node_modules/
site/
generated/
.DS_Store
news.xml
yarn.lock
venv
# Claude Code
# CLAUDE.md, AGENTS.md and the shared .claude/ commands+agents are committed so
# every contributor using AI inherits the project's review rules. Only local,
# machine-specific settings stay out of git.
.claude/settings.local.json
.claude/settings.json
.claude/worktrees/
